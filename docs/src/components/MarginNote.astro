---
/**
 * MarginNote — short editorial aside.
 *
 * On desktop (≥1100px), pulls into the right gutter when wrapped in
 * a `.has-margin` parent. On mobile, renders inline.
 */
---
<aside class="margin-note">
  <slot />
</aside>
