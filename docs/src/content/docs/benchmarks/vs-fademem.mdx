---
title: vs. FadeMem
description: A sober comparison with FadeMem under the stated LoCoMo protocol.
---

import GroupedBarChart from '../../../components/charts/GroupedBarChart';

## Head-to-head

<GroupedBarChart client:visible
  data={[
    { metric: 'Overall token F1', cogMem: 44.8, fadeMem: 27.2 },
    { metric: 'Multi-hop token F1', cogMem: 48.5, fadeMem: 29.4 },
  ]}
  xKey="metric"
  bars={[
    { dataKey: 'cogMem', name: 'Cognitive Memory', color: '#8b5cf6' },
    { dataKey: 'fadeMem', name: 'FadeMem', color: '#10b981' },
  ]}
  yDomain={[0, 55]}
/>

## What this comparison says

FadeMem is important because it takes forgetting seriously. It moves beyond static vector memory by adding temporal decay. Cognitive Memory builds on a similar intuition but treats decay as one part of a larger lifecycle: reinforcement, associations, cold storage, deep recall, and consolidation.

Under the cited LoCoMo protocol, Cognitive Memory scores higher overall and on multi-hop. The result should be read as evidence for the lifecycle stack, not as a blanket claim that every application needs that stack.

## Architectural difference

FadeMem focuses on fading memory over time. Cognitive Memory adds:

- retrieval-based strengthening, so recalled memories can become more stable
- association links, so related memories can travel together
- core promotion, so frequently used cross-session facts get protected
- hot/cold/stub tiers, so normal search stays cleaner
- deep recall, so cold and superseded originals can be searched when exact details matter

## Fair comparison notes

- FadeMem results are cited as published baseline numbers.
- Earlier docs overclaimed that some FadeMem capacity/dormancy details were undisclosed; those claims have been removed.
- This page compares reported benchmark behavior and design emphasis. It does not claim a full implementation audit of FadeMem.
